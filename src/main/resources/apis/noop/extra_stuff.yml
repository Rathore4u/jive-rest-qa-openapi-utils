---
get:
  tags:
  - noop
  operationId: noopExtraStuff
  description: this api is only a placeholder in order to let the generator generate some entity
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            "$ref": ../models/GenericEntities.yml#/GenericEntities
