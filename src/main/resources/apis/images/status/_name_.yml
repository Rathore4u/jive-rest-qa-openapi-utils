---
get:
  tags:
  - images
  operationId: getDefaultStatusLevelImage
  description: Return the binary image content of the default status level image for
    the specified name.
  parameters:
  - in: path
    name: name
    required: true
    schema:
      type: string
  responses:
    '200':
      description: Success
