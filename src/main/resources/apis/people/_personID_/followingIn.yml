---
get:
  tags:
  - person
  operationId: getFollowingIn
  description: Return a list of Streams in which the requesting user is following
    this person (if any).
  parameters:
  - in: query
    name: fields
    required: false
    schema:
      type: string
  - in: path
    name: personID
    required: true
    schema:
      type: string
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              "$ref": ../models/Stream.yml#/Stream
post:
  tags:
  - person
  operationId: setFollowingIn
  description: Replace the list of Streams in which the requesting user is following
    this person.
  parameters:
  - in: query
    name: fields
    required: false
    schema:
      type: string
  - in: path
    name: personID
    required: true
    schema:
      type: string
  requestBody:
    content:
      application/json:
        schema:
          type: array
          items:
            "$ref": ../models/Stream.yml#/Stream
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              "$ref": ../models/Stream.yml#/Stream
