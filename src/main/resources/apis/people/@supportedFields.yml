---
get:
  tags:
  - person
  operationId: getSupportedFields
  description: Return the set of fields for our object type that are supported in
    this Jive instance.
  parameters: 
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
